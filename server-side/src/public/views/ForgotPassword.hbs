<!DOCTYPE html>
<html lang="en">
    <head>
    <title>{{title}}</title>
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8" />
    <style>
        body {
            background-color: #ffffff;
            font-family: Arial, sans-serif;
            font-size: 16px;
            line-height: 1.4;
            color: #333333;
            margin: 0;
            padding: 0;
		}
        .container {
            max-width: 500px;
            margin: 0 auto;
            padding: 20px;
            text-align: center;
            border: 2px solid #e5e5e5;
            border-radius: 8px;
        }
        .logo {
            max-width: 200px;
            margin-bottom: 15px;
        }
        .title {
            color: #404040;
            font-size: 20px;
            font-weight: 600;
            text-align: center;
            margin-bottom: 20px;
        }

        .divider {
            max-width: 500px;
            margin: 0 auto;
            height: 2px;
            background: #e5e5e5;
        }
        .content {
            font-size: 16px;
            margin-bottom: 20px;
        }
        .support {
			font-size: 14px;
			color: #999999;
			margin-top: 20px;
		}
        .link {
            color: #404040;
            font-weight: bold;
        }
        /*a {
            color: #404040;
            font-weight: bold;
            text-decoration: none;
        }*/
        /*.link .btn-title {
            background: #0077B5;
            text-decoration: none;
            color: #fff;
            font-weight: 500;
            font-size: 25px;
            width: 300px;
            text-align: center;
            justify-content: center;
            padding: 10px 0;
            margin: 0 auto;
            border-radius: 8px; 
        }
        .link .btn-title:hover {
            opacity: 0.8;
        }*/
    </style>
    </head>
    <body>
        <div class="container">
            <a href="http://localhost:3000">
                <img class="logo" src="cid:icon" />
            </a>
            <div class="title">{{title}}</div>
            <div class="divider" />
            <div class="content">
                <p>To complete your resetting password for <span class="em">{{name}}</span>, please use the following OTP (One-Time Password) code:</p>
                <h2 class="link">{{code}}</h2>
                <p>
                    This OTP code is valid for 5 minutes. Make sure to change the password within these minutes, 
                    otherwise you will have to generate another link to enable the change of your password.
                </p>
            </div>
            <div class="support">
				If you have any questions or need assistance, please feel free to reach out to us at 
				<a href="mailto:grooveit96@gmail.com">grooveit96@gmail.com</a>. 
				We are here to help!
			</div>
        </div>
    </body>
</html>
