<!DOCTYPE html>
<html lang="en">
    <head>
    <title>{{title}}</title>
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8" />
    <style>
        body {
            font-family: calibri, 'DejaVu Sans', Arial, Helvetica, sans-serif;
            background-color: #cbd5e1;
        }
        .container {
            margin: 0 auto;
            width: 400px;
            padding: 10px 30px 30px 30px;
            box-shadow: 0 0 4px #ddd;
            background-color: #eef2f6;
            border-radius: 10px;
        }
        .content {
            width: 410px;
        }
        .link,
        a {
            border: solid 1px #0077B5;
            background: #0077B5;
            text-decoration: none;
            color: #fff;
            font-weight: bold;
            font-size: 25px;
            text-align: center;
            padding: 10px 0;
            margin: 0 auto;
            border-radius: 5px; 
        }
        .text-blue {
            color: #0077B5;
        }
    </style>
    </head>
    <body>
        <div class="container">
            <h1 class="text-blue" style="text-align: center;">{{title}}</h1>
            <p class="content">{{subject}}</p>
            <p class="link">
                <a href={{url}} style="color: #fff;">{{urlTitle}}</a>
            </p>
            <p>
                Regards, <br />
                <em>The GroooveIT Team.</em>
            </p>
        </div>
    </body>
</html>
