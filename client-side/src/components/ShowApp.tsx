import { FC } from "react";
import { ShowAppProps } from "../types";

export const ShowApp: FC<ShowAppProps> = () => {
  return (
    <div className="h-[500px] w-[500px] bg-richblack-700 rounded-xl"></div>
  );
};
